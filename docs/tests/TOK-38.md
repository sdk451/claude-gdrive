# TOK-38 — Test plan: S4.2 plugin skill (P-03)

## Cases

1. **`skills/gdrive-usage.md` exists** under repo root `skills/`.
2. **Drive search guidance** — body mentions Drive **`q`** / **`files.list`** (or MCP **`search_files`**) and illustrates at least **`mimeType`** and **`name`** style filters.
3. **Canonical doc link** — includes `https://developers.google.com/drive/api/guides/search-files` so users can verify operators against Google’s current documentation.

## Out of scope

- Slash command markdown files (P-04 deferred).
- Automated ZIP / Cowork install tests.
