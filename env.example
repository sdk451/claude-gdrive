# Runtime configuration for the gdrive-cowork-connector MCP server.
# Copy to .env (gitignored) and adjust per environment.

# Port the HTTP server listens on. Cloud Run injects $PORT automatically.
PORT=3000

# Future stories add: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SESSION_SECRET,
# REDIS_URL, SENTRY_DSN. Tracked in S0.4 (secrets & config management).
